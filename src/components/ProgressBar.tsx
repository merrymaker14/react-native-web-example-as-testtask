import React from "react";
import { View, Text } from "react-native";

const ProgressBar: React.FC = () => {
  return (
    <View>
      <Text>It's used a library for a time</Text>
    </View>
  );
};

export default ProgressBar;